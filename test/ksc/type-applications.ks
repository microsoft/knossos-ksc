(def f Float (t : Tuple Float Integer) 0.0)

(def main Integer ()
     (let (f_     (f @ (Tuple Float Integer) 0.0 0))
     (let (fwd$f_ (fwd$f @ (Tuple Float Integer) (tuple 0.0 0) (tuple 1.0 (tuple))))
     (let (rev$f_ (rev$f @ (Tuple Float Integer) (tuple 0.0 0) 1.0))
       1))))
