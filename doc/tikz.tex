\documentclass{standalone}
\usepackage{freetikz}
\usetikzlibrary{calc}
\begin{document}
\begin{tikzpicture}
  \node[dot,minimum size=25] (plus) at (0, 0) {$+$};
  \node[dot,minimum size=25] (dup) at ($ (plus) + (0, -2) $) {dup};
  \draw[->] ($ (plus) + (-1.5, -0.5) $) to[out=0, in=-135] (plus);
  \draw[->] ($ (plus) + (-1.5, +0.5) $) to[out=0, in=+135] (plus);
  \draw[->] (plus) to ($ (plus) + (1.5, 0) $);

  \draw[->] ($ (dup) + (1.5, -0.5) $) to[out=180, in=-45] (dup);
  \draw[->] ($ (dup) + (1.5, +0.5) $) to[out=180, in=+45] (dup);
  \draw[->] (dup) to[out=180, in=0] ($ (dup) + (-1.5, 0) $);

  \draw[->] [dashed] (plus) to[] node[right] {()} (dup);

  \node[dot,minimum size=25] (fcomp) at ($ (plus) + (5, 0) $) {$f$};
  \node[dot,minimum size=25] (gcomp) at ($ (fcomp) + (2, 0) $) {$g$};

  \node[dot,minimum size=25] (dfcomp) at ($ (fcomp) + (0, -2) $) {$df$};
  \node[dot,minimum size=25] (dgcomp) at ($ (gcomp) + (0, -2) $) {$dg$};

  \draw[->] ($ (fcomp) + (-1.5, 0) $) to (fcomp);
  \draw[->] (fcomp) to (gcomp);
  \draw[->] (fcomp) to ($ (gcomp) + (1.5, 0) $);

  \draw[->] [dashed] (fcomp) to (dfcomp);
  \draw[->] [dashed] (gcomp) to node[right] {$(T_f, T_g)$} (dgcomp);

  \draw[->] (dfcomp) to ($ (dfcomp) + (-1.5, 0) $);
  \draw[->] (dgcomp) to (dfcomp);
  \draw[->] ($ (dgcomp) + (1.5, 0) $) to (dfcomp);

  \node[dot,minimum size=25] (fpar) at ($ (plus) + (0, -5) $) {$f$};
  \node[dot,minimum size=25] (gpar) at ($ (fpar) + (1, -1) $) {$g$};

  \node[dot,minimum size=25] (dfpar) at ($ (fpar) + (0, -3) $) {$df$};
  \node[dot,minimum size=25] (dgpar) at ($ (gpar) + (0, -3) $) {$dg$};

  \draw[->] ($ (fpar) + (-1.5, 0) $) to (fpar);
  \draw[->] (fpar) to ($ (fpar) + (2.5, 0) $);
  \draw[->] ($ (gpar) + (-2.5, 0) $) to (gpar);
  \draw[->] (gpar) to ($ (gpar) + (1.5, 0) $);

  \draw[->] [dashed] (fpar) to (dfpar);
  \draw[->] [dashed] (gpar) to node[right] {$(T_f, T_g)$} (dgpar);

  \draw[->]  (dfpar) to ($ (dfpar) + (-1.5, 0) $);
  \draw[->] ($ (dfpar) + (2.5, 0) $) to (dfpar);
  \draw[->] (dgpar) to ($ (dgpar) + (-2.5, 0) $);
  \draw[->] ($ (dgpar) + (1.5, 0) $) to (dgpar);

\end{tikzpicture}
\end{document}


% https://tex.stackexchange.com/questions/48756/tikz-relative-coordinates
