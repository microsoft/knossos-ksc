{
  "train_exprs": "summations(n=50,min_terms=51,max_terms=100,choose_from=100)",
  "test_exprs": "summations(n=100,min_terms=51,max_terms=100)",
  "rules": "simplify_rules",
  "train_search": "beam",
  "max_gnn_train": 3000,
  "eval_search": "beam",
  "max_gnn_eval": 1000,
  "max_epochs": 10,
  "patience_epochs": 3,
  "total_train_time": 72000,
  "simulation_depth_train": 200,
  "simulation_depth_eval": 200,
  "dataset_refiners": ["reuse_refiner"],
  "oracle": false,
  "extra_plots": ["summations_fit_animated"],
  "sparse_gnn": true
}
